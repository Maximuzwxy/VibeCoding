# 项目开发规则 a

## 一、技术栈

### 1.1 HTML + Flask + JSON 项目

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML + CSS + JavaScript | 页面结构、样式、交互 |
| 后端 | Flask | Python Web 框架 |
| 数据存储 | JSON 文件 | 轻量级数据持久化 |

### 1.2 项目结构示例
```
project/
├── app.py              # Flask 主应用
├── templates/          # HTML 模板
│   └── index.html
├── static/             # 静态资源
│   ├── css/
│   ├── js/
│   └── images/
├── data/               # JSON 数据文件
│   └── db.json
└── tests/              # 测试代码
```

### 1.3 常用命令
```bash
# 启动服务
python app.py

# 安装依赖
pip install flask markdown
```

### 1.4 注意事项
- JSON 数据库适合小规模数据存储
- 注意处理 JSON 文件的读写并发问题
- 生产环境建议使用正式数据库（SQLite/MySQL/PostgreSQL）

---

## 二、代码与测试

### 2.1 开发顺序
- 先完成功能代码开发
- 再完成对应的测试代码
- 测试代码包括：测试函数、测试数据

### 2.2 测试覆盖
- 每次实现功能时，必须配套完成对应的测试代码
- 测试范围取决于本次实现的功能内容
- 不得交付只有代码没有测试的功能

### 2.3 测试文件位置
```
tests/
├── api/              # API 测试
│   ├── test_*.py
│   └── ...
├── ui/               # UI 测试
│   ├── test_*.py
│   └── ...
├── conftest.py       # 公共 fixtures
└── run_all_tests.py  # 全量测试入口
```

### 2.4 测试类型与框架
| 类型 | 说明 | 测试框架 |
|------|------|----------|
| API 测试 | 验证后端接口正确性 | pytest |
| UI 测试 | 验证前端页面功能 | pytest + Playwright |

### 2.5 测试框架说明
- **pytest**：Python 单元测试框架，用于后端 API 测试
- **Playwright**：端到端测试框架，用于前端 UI 测试（浏览器自动化）
- **headless 模式**：
  - `headless=True`：浏览器在后台运行，不可见
  - `headless=False`：浏览器可见，会弹出来（开发调试时使用）
- 运行 UI 测试前需要启动服务器：`python app.py`
- 运行测试命令：`pytest tests/` 或 `pytest tests/api/` / `pytest tests/ui/`

---

## 三、自测要求

### 3.1 功能自测
- 每个功能开发完成后，必须运行自动化测试验证
- 发现问题自行修复，修复后再次测试，直到通过

### 3.2 自测流程
1. 运行功能对应的测试代码
2. 如有失败，依次排查：
   - 测试代码本身是否有问题
   - 测试数据是否有问题
   - 实际代码逻辑是否有问题
3. 修复问题后重新测试
4. 测试全部通过后再交付

---

## 四、问题修复

### 4.1 用户反馈的问题
- 如果是页面的微调：直接修复
- 如果是需求改动或 Bug：修复代码的同时，必须补齐对应的测试代码

### 4.2 测试代码同步
- 任何代码改动，都需要同步更新相关的测试代码
- 确保改动后的功能有测试覆盖

---

## 五、系统测试

### 5.1 定义
- 系统测试 = 运行所有功能对应的测试代码
- 包含所有 API 测试 + 所有 UI 测试

### 5.2 执行时机
- 当用户明确要求时执行全量系统测试
- 开发过程中不需要自动运行全量测试

---

## 六、禁止行为

- 自作主张添加未规划的功能
- 跨越阶段开发（如有阶段规划）
- 交付没有测试代码的功能
- 把用户当测试员

---

*文档版本：1.0*
*更新日期：2026-05-14*
