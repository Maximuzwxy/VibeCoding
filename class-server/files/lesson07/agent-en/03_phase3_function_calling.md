# Phase 3 — Add Function Calling (System Prompt)

## Goal
Implement the first Function Call

## Features
- `update_system_prompt` — Update the system prompt / role settings

## Use Cases
- Set the AI's role (e.g., "You are a programming assistant")
- Customize the AI's behavior and tone
